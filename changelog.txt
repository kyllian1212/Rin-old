------------------------------------------------------------------------
v0.1.0 -----------------------------------------------------------------
* initial release with report function
v0.1.1 ----------------------------------------------------------------- 
* bugfix with role issues
v0.1.2 -----------------------------------------------------------------
* bugfix with crash issue + added songs into the presence thing

------------------------------------------------------------------------
v0.2.0 -----------------------------------------------------------------
* added more songs into the library
* added a proper crash handler
v0.2.1 -----------------------------------------------------------------
* converted the !!quit command into a proper bot.command() instead of a 
on_message() command
* .gitignore update to exclude the python cache

------------------------------------------------------------------------
v0.3.0 -----------------------------------------------------------------
* added !!say command
* added !!info command
* added !!october18 command
* transferred the version into an .env variable
* the bot will now show when it's in dev mode (as in when it's running 
locally, rather than running on heroku)
v0.3.1 -----------------------------------------------------------------
* fixed the !!info command (everyone used to create the bot lol)